# Copy this file to .env and set secure values for production.
# Example values are included for local development only.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=nws

# Compose-friendly DATABASE_URL using the values above. Override if using external DB.
DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# If you use an external Postgres/PostGIS instance, set DATABASE_URL directly, for example:
# DATABASE_URL=postgresql+psycopg2://myuser:mypassword@db.example.com:5432/mydb

# Host port configuration for the FastAPI app container.
# Set to an uncommon port to avoid conflicts, e.g. 18080
APP_PORT=18080
# Optional: bind the host port to a specific interface (e.g. 127.0.0.1)
# Leave empty to bind on all interfaces.
APP_HOST_BIND=127.0.0.1

# Poll interval for fetching NWS alerts (seconds). Default 60.
POLL_INTERVAL=60

# Admin API key for authenticated ingest endpoint (set to a strong secret)
ADMIN_API_KEY=
